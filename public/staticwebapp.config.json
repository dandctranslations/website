{
  "routes": [
    { "route": "/api/send-invoice", "allowedRoles": ["administrator"] }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/static/*", "*.{css,js,ico,png,svg,json,txt,woff,woff2}"]
  }
}
